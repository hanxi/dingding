[project]
name = "dingding"
version = "0.1.0"
description = "钉钉机器人库 - 支持异步消息推送和接收"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "aiohttp>=3.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]
